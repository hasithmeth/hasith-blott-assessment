/**
 * Credentials of a user
 * @property {string} firstName - First name of the user. (Required)
 * @property {string} lastName - Last name of the user. (Required)
 */
interface User {
  firstName: string;
  lastName: string;
}
